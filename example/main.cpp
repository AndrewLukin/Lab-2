#include <iostream>
#include "calculator.hpp"

using namespace std;

int main()
{
	double a=4, b=3;
	int c=5;
			cout << sum(a, b) << endl;
			cout << sub(a, b) << endl;
			cout << multip(a, b) << endl;
			cout << di(a, b) << endl;
			cout << power(a, c) << endl;
			cout << squareroot(a) << endl;
 	system("pause");
	return 0;
}
